services:
  ollama:
    image: ollama/ollama:latest
    container_name: local_llm_server
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    restart: always

volumes:
  ollama_data: